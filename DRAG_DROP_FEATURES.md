# Улучшения UI/UX - Drag & Drop и Статусы Загрузки

## Реализованные улучшения

### 1. MaterialUploader - Загрузка материалов для проектов

**Расположение:** `frontend/src/components/MaterialUploader.tsx`

**Возможности:**
- ✅ Drag & Drop для изображений и видео
- ✅ Множественная загрузка (до 10 файлов за раз)
- ✅ Визуальные индикаторы состояния:
  - 🔄 Uploading - показывает прогресс загрузки
  - ✅ Success - зеленая галочка при успешной загрузке
  - ❌ Error - красный значок при ошибке
- ✅ Превью изображений во время загрузки
- ✅ Анимированные переходы состояний
- ✅ Удаление файлов с ошибками

**Использование:**
```tsx
import MaterialUploader, { UploadPreview } from '@/components/MaterialUploader'

<MaterialUploader
  onFilesSelected={handleFilesSelected}
  maxFiles={10}
  accept="image/*,video/*"
  disabled={isUploading}
/>

<UploadPreview
  uploadingFiles={uploadingFiles}
  onRemove={handleRemoveUpload}
/>
```

**Интеграция:**
- Используется на Этапе 3 создания проекта
- Показывает статус каждого загружаемого файла
- Автоматически удаляет успешно загруженные файлы через 2 секунды

---

### 2. ImageDropzone - Универсальный компонент для одиночных изображений

**Расположение:** `frontend/src/components/ImageDropzone.tsx`

**Возможности:**
- ✅ Drag & Drop для одного изображения
- ✅ Превью загруженного изображения
- ✅ Кнопка "Изменить" поверх превью
- ✅ Кнопка "Удалить" (красный X)
- ✅ Визуальная обратная связь при перетаскивании
- ✅ Анимированные переходы
- ✅ Поддержка disabled состояния

**Использование:**
```tsx
import ImageDropzone from '@/components/ImageDropzone'

<ImageDropzone
  label="Фронтальное изображение"
  value={previewUrl}
  onChange={(file) => handleImageChange(file)}
  onClear={() => handleImageClear()}
  disabled={isLoading}
  height="h-48" // опционально, по умолчанию h-48
/>
```

**Интеграция:**
- Используется в BloggerModal для загрузки:
  - Фронтального изображения блогера
  - Изображения блогера в локации
- Заменяет старый input[type=file] с улучшенным UX

---

## Технические детали

### Отслеживание состояния загрузки

**CreatePage.tsx:**
```tsx
const [uploadingFiles, setUploadingFiles] = useState<UploadingFile[]>([])

const handleFilesSelected = async (files: File[]) => {
  // 1. Создаем объекты отслеживания с превью
  const newUploads = files.map(file => ({
    id: `${Date.now()}-${Math.random()}`,
    file,
    preview: URL.createObjectURL(file),
    status: 'uploading',
    progress: 0,
  }))
  
  setUploadingFiles(prev => [...prev, ...newUploads])
  
  // 2. Загружаем файлы последовательно
  for (const upload of newUploads) {
    try {
      // Симуляция прогресса
      const progressInterval = setInterval(() => {
        setUploadingFiles(prev =>
          prev.map(u =>
            u.id === upload.id && u.progress < 90
              ? { ...u, progress: u.progress + 10 }
              : u
          )
        )
      }, 200)
      
      // Загрузка на сервер
      await uploadMaterialMutation.mutateAsync(...)
      
      clearInterval(progressInterval)
      
      // 3. Отмечаем как успешно загруженный
      setUploadingFiles(prev =>
        prev.map(u =>
          u.id === upload.id
            ? { ...u, status: 'success', progress: 100 }
            : u
        )
      )
      
      // 4. Удаляем через 2 секунды
      setTimeout(() => {
        setUploadingFiles(prev => prev.filter(u => u.id !== upload.id))
      }, 2000)
      
    } catch (error) {
      // Отмечаем ошибку
      setUploadingFiles(prev =>
        prev.map(u =>
          u.id === upload.id
            ? { ...u, status: 'error', error: error.message }
            : u
        )
      )
    }
  }
}
```

---

## Визуальные эффекты

### Анимации перетаскивания
- **Border**: меняется с `border-white/20` на `border-blue-500`
- **Background**: добавляется `bg-blue-500/10`
- **Scale**: увеличивается до `scale-105`
- **Icon color**: меняется с `text-white/40` на `text-blue-400`

### Статусы загрузки
- **Uploading**: вращающийся Loader2 + процент загрузки
- **Success**: зеленая галочка CheckCircle
- **Error**: красный значок AlertCircle + сообщение об ошибке

### Transitions
- Все переходы используют `transition-all duration-300`
- Hover эффекты: `hover:border-blue-500/50 hover:bg-white/5`
- Group hover для кнопок: `group-hover:opacity-100`

---

## Будущие улучшения

### Приоритет 1 (Текущая сессия)
- [ ] Добавить drag & drop в остальные input поля
- [ ] Улучшить обработку ошибок с retry функционалом
- [ ] Добавить валидацию размера файлов (max 10MB)

### Приоритет 2 (Следующая сессия)
- [ ] Drag & drop для текстовых полей (сценарий)
- [ ] Прогресс-бар для больших файлов
- [ ] Batch upload optimization
- [ ] Компрессия изображений на клиенте

### Приоритет 3 (Опционально)
- [ ] Предпросмотр видео перед загрузкой
- [ ] Crop/resize изображений
- [ ] Поддержка paste из буфера обмена
- [ ] Undo/Redo для удаленных файлов

---

## Совместимость

- ✅ React 19.1
- ✅ TypeScript 5.9
- ✅ Tailwind CSS 4.1
- ✅ lucide-react 0.469
- ✅ Все современные браузеры (Chrome, Firefox, Safari, Edge)

## Производительность

- Object URLs создаются для превью (`URL.createObjectURL()`)
- URLs очищаются при размонтировании компонента
- Загрузка файлов происходит последовательно (не параллельно) для контроля нагрузки
- Успешно загруженные превью автоматически удаляются через 2 секунды

---

**Дата создания:** 4 ноября 2025  
**Версия:** 1.0
